# Balancer Bot PID Configuration
# Modifying this file requires application restart (or reload logic)

# --- Rate Controller (Inner Loop) ---
rate_P               = 0.3
rate_I               = 0.0
rate_D               = 0.6
rate_I_lim           = 0.15
rate_FF              = 0.0

# --- Physics-Based Outer Loop ---
outer_k_pos          = 0
outer_k_vel          = -0.00015
outer_k_pitch        = 12.0
outer_k_pitch_rate   = 0.25

# --- Trim / Bias Learning ---
angle_I              = 0
lean_trim_I          = 0.04
lean_trim_max_deg    = 4
lean_trim_decay_s    = 30

# --- Legacy Outer-Loop Keys (load-only compatibility) ---
# Older configs may still use:
#   angle_to_rate_k, vel_P, vel_I, vel_D, vel_I_lim, pos_P
# The rewritten controller reads those keys if present, but new configs should
# only tune the outer_k_* fields above.
